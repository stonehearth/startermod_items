#include "pch.h"
#include "client/client.h"
#include "trace_entity.h"

using namespace radiant;
using namespace radiant::client;


void TraceEntitiesCmd::operator()(PendingCommandPtr cmd)
{
   ASSERT(false);
}

void TraceEntityCmd::operator()(PendingCommandPtr cmd)
{
   ASSERT(false);
}

